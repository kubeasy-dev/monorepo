---
phase: 09-ui-parity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/_protected/dashboard.tsx
  - apps/web/src/components/dashboard-chart.tsx
autonomous: true
requirements: [PARITY-04]

must_haves:
  truths:
    - "Dashboard stats cards use Award/Trophy/Star/Flame icons matching ../website DashboardStats component"
    - "Dashboard stats cards show labels 'Completed'/'Points'/'Rank'/'Day Streak' matching reference"
    - "Dashboard chart + recent activity are in a side-by-side 2-column grid at lg breakpoint"
    - "Dashboard Quick Actions use Button variant=secondary asChild wrapping Link"
    - "DashboardChart insight box uses bg-secondary rounded-xl with text-center"
  artifacts:
    - path: "apps/web/src/routes/_protected/dashboard.tsx"
      provides: "Dashboard page with reference-matching stats icons, labels, grid layout, and Button quick actions"
    - path: "apps/web/src/components/dashboard-chart.tsx"
      provides: "Dashboard chart with reference-matching insight box styling"
  key_links:
    - from: "apps/web/src/routes/_protected/dashboard.tsx"
      to: "@kubeasy/ui/button"
      via: "import Button for Quick Actions"
      pattern: "import.*Button.*from.*@kubeasy/ui/button"
---

<objective>
Restore visual parity for dashboard and profile pages between `apps/web` and `../website`.

Purpose: The dashboard has 6 concrete differences: wrong stat card icons/labels, missing 2-column grid for chart+activity, wrong chart insight box styling, and Quick Actions using raw Links instead of Button components. Profile is structurally identical and requires no changes.

Output: Dashboard page with correct icons, labels, grid layout, Button quick actions, and chart insight styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-parity/09-CONTEXT.md
@.planning/phases/09-ui-parity/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard stats icons, labels, layout, and Quick Actions</name>
  <files>apps/web/src/routes/_protected/dashboard.tsx</files>
  <read_first>
    - ../website/components/dashboard-stats.tsx (reference — Award/Trophy/Star/Flame icons, card labels, layout structure)
    - ../website/app/(main)/dashboard/page.tsx (reference — 2-column grid for chart+activity, Button quick actions)
    - apps/web/src/routes/_protected/dashboard.tsx (current dashboard — wrong icons, stacked layout, raw Link quick actions)
  </read_first>
  <action>
In `apps/web/src/routes/_protected/dashboard.tsx`, make these targeted fixes:

1. **Fix stat card icons and labels** — Replace the current icon imports and card content to match `../website/components/dashboard-stats.tsx`:
   - Change icon imports from `Clock, Target, TrendingUp, Trophy` to `Award, Flame, Star, Trophy, Target, TrendingUp` (keep Target and TrendingUp for Quick Actions)
   - Card 1 (Completed): Change icon from `Trophy` to `Award`. Change label from "XP Earned" to "Completed". Change value from `{xpData?.xpEarned ?? 0}` to `{completion?.completedCount ?? 0}`. Change subtitle from `{xpData?.rank ?? "Beginner"}` to `{completion?.percentageCompleted ?? 0}% of all challenges`. Change icon container padding from `p-2` to `p-3`, add `neo-shadow rounded-lg`. Icon size from `h-5 w-5` to `w-6 h-6`. Label text style from `text-sm font-bold text-muted-foreground uppercase` to `text-sm font-bold text-foreground`. Value style add `text-foreground`. Remove uppercase from label.
   - Card 2 (Points): Change icon from `Target` to `Trophy`. Change label from "Completed" to "Points". Change value from `{completion?.completedCount ?? 0}` to `{xpData?.xpEarned ?? 0}`. Change subtitle from `of {completion?.totalCount ?? 0} challenges` to `Total XP earned`.
   - Card 3 (Rank): Change icon from `TrendingUp` to `Star`. Change label from "Progress" to "Rank". Change value from `{completion?.percentageCompleted ?? 0}%` to `{xpData?.rank ?? "Beginner"}`. Change value text size to `text-2xl font-black text-foreground leading-tight`. Change subtitle from "completion rate" to "Congratulations!".
   - Card 4 (Day Streak): Change icon from `Clock` to `Flame`. Keep label "Streak" but change to "Day Streak". Keep value `{streak?.currentStreak ?? 0}`. Change subtitle from "day streak" to "Keep it up!".
   - For ALL 4 cards: Change icon container from `<div className="p-2 bg-primary neo-border-thick">` to `<div className="p-3 bg-primary neo-border-thick neo-shadow rounded-lg">`. Change icon size from `h-5 w-5` to `w-6 h-6`. Change the layout from icon+label in same flex row to: `<div className="flex items-center gap-4 mb-3"><div className="p-3 bg-primary neo-border-thick neo-shadow rounded-lg">{icon}</div><div><p className="text-sm font-bold text-foreground">{label}</p><p className="text-3xl font-black text-foreground">{value}</p></div></div><p className="text-sm font-bold text-foreground">{subtitle}</p>`.

2. **Fix chart + activity grid layout** — Wrap `<DashboardChart />` and `<DashboardRecentActivity />` in a 2-column grid:
   ```tsx
   <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-12">
     <DashboardChart />
     <DashboardRecentActivity />
   </div>
   ```
   Remove `mb-12` from DashboardChart's own root element (handled in Task 2 below). DashboardRecentActivity may also need its own `mb-12` removed if it has one — check the component file.

3. **Fix Quick Actions** — Replace raw `<Link>` elements with `<Button variant="secondary" ... asChild>`:
   - Add `import { Button } from "@kubeasy/ui/button"`
   - Replace each Quick Action:
     ```tsx
     <Button
       variant="secondary"
       className="neo-border neo-shadow font-black h-auto py-6 flex-col gap-2"
       asChild
     >
       <Link to="/challenges">
         <Target className="w-8 h-8" />
         <span>Browse Challenges</span>
       </Link>
     </Button>
     ```
   - Apply same pattern for "Explore Themes" (with TrendingUp icon, Link to="/themes") and "View on GitHub" (with Trophy icon, using `<a href={GITHUB_URL} target="_blank" rel="noreferrer">`)
   - Remove the old manual classes from the Link/a elements: `bg-secondary neo-border neo-shadow font-black py-6 flex flex-col items-center gap-2 hover:neo-shadow-lg transition-shadow`

Per D-06 (../website source of truth), per D-08 (fix in apps/web), per D-09 (touch packages/ui only for shared component issues).
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/routes/_protected/dashboard.tsx` imports `Award, Flame, Star` from lucide-react
    - `apps/web/src/routes/_protected/dashboard.tsx` does NOT import `Clock` from lucide-react (replaced by Flame)
    - `apps/web/src/routes/_protected/dashboard.tsx` contains `import { Button } from "@kubeasy/ui/button"`
    - `apps/web/src/routes/_protected/dashboard.tsx` contains `variant="secondary"` and `asChild` (Button quick actions)
    - `apps/web/src/routes/_protected/dashboard.tsx` contains `lg:grid-cols-2 gap-8 mb-12` (chart+activity grid)
    - `apps/web/src/routes/_protected/dashboard.tsx` contains the text "Points" (XP stat label matching reference)
    - `apps/web/src/routes/_protected/dashboard.tsx` contains the text "Congratulations!" (Rank subtitle matching reference)
    - `apps/web/src/routes/_protected/dashboard.tsx` contains the text "Keep it up!" (Streak subtitle matching reference)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Dashboard stats cards show Award/Trophy/Star/Flame icons with reference labels and layout. Chart and activity are side-by-side at lg. Quick Actions use Button components.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DashboardChart insight box styling</name>
  <files>apps/web/src/components/dashboard-chart.tsx</files>
  <read_first>
    - ../website/components/dashboard-chart.tsx (reference — insight box uses `bg-secondary neo-border-thick rounded-xl`, `text-sm font-bold text-center`)
    - apps/web/src/components/dashboard-chart.tsx (current — `bg-background neo-border-thick`, no rounded-xl, no text-center, has mb-12 on root)
  </read_first>
  <action>
In `apps/web/src/components/dashboard-chart.tsx`, make these fixes:

1. **Remove mb-12 from root element** — Change `className="bg-secondary neo-border-thick neo-shadow p-8 mb-12"` to `className="bg-secondary neo-border-thick neo-shadow p-8"`. The mb-12 is now handled by the parent grid in dashboard.tsx.

2. **Fix insight box styling** — Change the insight box div from:
   `<div className="mt-4 p-4 bg-background neo-border-thick">`
   to:
   `<div className="mt-4 p-4 bg-secondary neo-border-thick rounded-xl">`
   (matches reference: `bg-secondary` instead of `bg-background`, adds `rounded-xl`)

3. **Fix insight text styling** — Change the insight paragraph with best theme from:
   `<p className="font-bold">` ... `<span className="text-primary font-black">` ... `<span className="font-black">`
   to:
   `<p className="text-sm font-bold text-center">` ... `<span className="text-primary">` (just text-primary, no separate font-black on span)
   Match the reference pattern exactly:
   ```tsx
   <p className="text-sm font-bold text-center">
     Your best theme is{" "}
     <span className="text-primary">{bestThemeName}</span> at{" "}
     {bestTheme.percentageCompleted}%!
   </p>
   ```

4. **Fix fallback text styling** — Change the fallback paragraph from:
   `<p className="font-bold text-muted-foreground">`
   to:
   `<p className="text-sm font-bold text-center">`
   (matches reference: adds text-sm and text-center, removes text-muted-foreground)
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/dashboard-chart.tsx` root div does NOT contain `mb-12`
    - `apps/web/src/components/dashboard-chart.tsx` insight box contains `bg-secondary neo-border-thick rounded-xl` (not `bg-background`)
    - `apps/web/src/components/dashboard-chart.tsx` insight paragraph contains `text-sm font-bold text-center`
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>DashboardChart insight box uses bg-secondary with rounded-xl and centered text matching ../website. Root mb-12 removed (parent grid handles spacing).</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Dashboard page uses correct icon set (Award, Trophy, Star, Flame)
- Chart and activity are in a 2-column grid at lg breakpoint
- Quick Actions use Button components, not raw Links
- Chart insight box uses bg-secondary with rounded-xl
- Profile page: verified identical to reference (no changes needed)
- Per D-05: Run Playwright screenshot comparison of `/dashboard` and `/profile` between `apps/web` (port 3000) and `../website` (port 3001) using `with_server.py`:
  ```bash
  python .agents/skills/webapp-testing/scripts/with_server.py \
    --server "cd ../website && pnpm dev --port 3001" --port 3001 \
    --server "cd apps/web && pnpm dev" --port 3000 \
    -- python scripts/compare_dashboard_screenshots.py
  ```
  Note: Dashboard and profile are auth-protected pages. The comparison script must handle authentication (login first) or compare against mock/seeded data screenshots. If auth is not feasible in automated mode, capture `apps/web` screenshots only and note for manual comparison.
</verification>

<success_criteria>
Dashboard page in `apps/web` matches `../website` in stat card icons/labels, chart+activity grid layout, insight box styling, and Quick Actions button pattern. Profile page is already identical. Playwright screenshots confirm visual parity. PARITY-04 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-parity/09-04-SUMMARY.md`
</output>
