---
phase: 09-ui-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/hero-section.tsx
  - apps/web/src/components/stats-section.tsx
  - apps/web/src/components/features-section.tsx
  - apps/web/src/components/how-it-works-section.tsx
  - apps/web/src/components/open-source-section.tsx
  - apps/web/src/components/early-access-section.tsx
  - apps/web/src/components/cta-section.tsx
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "Landing page sections in apps/web are visually identical to ../website — same Tailwind classes, same structure"
    - "No visual differences remain between the 7 landing page section components"
  artifacts:
    - path: "apps/web/src/routes/index.tsx"
      provides: "Landing page composing 7 sections"
    - path: "apps/web/src/components/hero-section.tsx"
      provides: "Hero section matching ../website"
  key_links:
    - from: "apps/web/src/routes/index.tsx"
      to: "apps/web/src/components/hero-section.tsx"
      via: "import HeroSection"
      pattern: "import.*HeroSection"
---

<objective>
Verify and fix visual parity for the marketing landing page between `apps/web` and `../website`.

Purpose: Per D-01, "marketing pages" = landing page only. Research indicates the 7 section components were ported during Phase 4 and are likely identical, but a code diff pass is required to confirm and fix any remaining class differences.

Output: All 7 landing page section components verified or corrected to match ../website.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-parity/09-CONTEXT.md
@.planning/phases/09-ui-parity/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff and fix all 7 landing page section components</name>
  <files>apps/web/src/components/hero-section.tsx, apps/web/src/components/stats-section.tsx, apps/web/src/components/features-section.tsx, apps/web/src/components/how-it-works-section.tsx, apps/web/src/components/open-source-section.tsx, apps/web/src/components/early-access-section.tsx, apps/web/src/components/cta-section.tsx</files>
  <read_first>
    - ../website/components/hero-section.tsx (reference)
    - ../website/components/features-section.tsx (reference)
    - ../website/components/how-it-works-section.tsx (reference)
    - ../website/components/open-source-section.tsx (reference)
    - ../website/components/early-access-section.tsx (reference)
    - ../website/components/cta-section.tsx (reference)
    - apps/web/src/components/hero-section.tsx (target)
    - apps/web/src/components/stats-section.tsx (target)
    - apps/web/src/components/features-section.tsx (target)
    - apps/web/src/components/how-it-works-section.tsx (target)
    - apps/web/src/components/open-source-section.tsx (target)
    - apps/web/src/components/early-access-section.tsx (target)
    - apps/web/src/components/cta-section.tsx (target)
  </read_first>
  <action>
For each of the 7 section components, perform a systematic code diff between `../website/components/{name}.tsx` and `apps/web/src/components/{name}.tsx`:

1. **HeroSection** — Compare Tailwind classes, text content, layout structure. Fix any divergent classes.
2. **StatsSection** — Note: `../website` may not have a separate `stats-section.tsx`; check if stats are part of the landing page or a separate component. Compare whatever exists.
3. **FeaturesSection** — Compare card layout, icon usage, class names.
4. **HowItWorksSection** — Compare step layout, numbering, interactive terminal embed.
5. **OpenSourceSection** — Compare GitHub link styling, badge layout.
6. **EarlyAccessSection** — Compare CTA button, form layout.
7. **CTASection** — Compare final call-to-action section.

For each component:
- Replace any Next.js-specific patterns: `<Image>` to `<img>`, `<Link href=...>` to `<Link to=...>` or `<a href=...>`
- Match Tailwind classes exactly to the reference
- Match text content exactly to the reference
- Keep any TanStack Router-specific patterns (e.g., `<Link to=...>` instead of `href`)

If a component is already identical (no diff), leave it untouched. Only modify files with actual differences.

**Splitting guidance:** If during the diff pass, more than 2 components require substantial changes (not just minor class tweaks but structural layout changes), pause after completing those 2, commit, and continue with the rest. This keeps context manageable. If all changes are minor class fixes, complete all 7 in a single pass.

Per D-01 (landing page only), per D-06 (../website source of truth), per D-08 (fix in apps/web).
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` passes
    - No `next/image` imports remain in any of the 7 section components
    - No `next/link` imports remain in any of the 7 section components
    - Each component's Tailwind classes match its ../website counterpart (verified during implementation by the executor reading both files)
  </acceptance_criteria>
  <done>All 7 landing page section components in apps/web match ../website visually. Any class differences found during the diff pass are corrected. PARITY-02 is satisfied.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Landing page at `/` renders all 7 sections without errors
- Per D-05: Run Playwright screenshot comparison of `/` between `apps/web` (port 3000) and `../website` (port 3001) using `with_server.py`:
  ```bash
  python .agents/skills/webapp-testing/scripts/with_server.py \
    --server "cd ../website && pnpm dev --port 3001" --port 3001 \
    --server "cd apps/web && pnpm dev" --port 3000 \
    -- python scripts/compare_marketing_screenshots.py
  ```
  The comparison script should capture `/` from both apps at desktop (1280x800) and mobile (375x812) viewports, save to `/tmp/parity-09-02/` for visual diff review.
</verification>

<success_criteria>
Marketing landing page in `apps/web` matches `../website` visually across all 7 section components. Playwright screenshots confirm visual parity. PARITY-02 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-parity/09-02-SUMMARY.md`
</output>
