---
phase: 09-ui-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/blog/index.tsx
  - apps/web/src/routes/blog/$slug.tsx
  - apps/web/src/components/blog-card.tsx
  - apps/web/src/components/author-card.tsx
  - apps/web/src/components/related-posts.tsx
  - apps/web/src/components/table-of-contents.tsx
autonomous: true
requirements: [PARITY-01]

must_haves:
  truths:
    - "Blog list page shows featured (pinned) card with full-width layout, cover image, author/date footer, ArrowRight CTA"
    - "Blog list page shows compact cards with category+date header bar, ArrowRight in footer, hover:shadow-none"
    - "Blog list page has category filter badges derived from post list"
    - "Blog article page wraps content in prose-neo class for proper typography"
    - "Blog article page has category badge as a clickable link to category page"
    - "Blog article page uses grid layout with sidebar ToC on desktop (lg:grid-cols-[1fr_250px])"
    - "Blog article page shows AuthorCard component below content"
    - "Blog article page shows RelatedPosts component below AuthorCard"
  artifacts:
    - path: "apps/web/src/components/blog-card.tsx"
      provides: "BlogCard component with featured/compact variants matching reference"
      exports: ["BlogCard"]
    - path: "apps/web/src/components/author-card.tsx"
      provides: "AuthorCard component matching reference"
      exports: ["AuthorCard"]
    - path: "apps/web/src/components/related-posts.tsx"
      provides: "RelatedPosts component matching reference"
      exports: ["RelatedPosts"]
    - path: "apps/web/src/components/table-of-contents.tsx"
      provides: "TableOfContentsClient component matching reference"
      exports: ["TableOfContentsClient"]
    - path: "apps/web/src/routes/blog/index.tsx"
      provides: "Blog list page using BlogCard with pinned/regular separation and category filters"
    - path: "apps/web/src/routes/blog/$slug.tsx"
      provides: "Blog article page with prose-neo wrapper, grid layout, sidebar ToC, AuthorCard, RelatedPosts"
  key_links:
    - from: "apps/web/src/routes/blog/index.tsx"
      to: "apps/web/src/components/blog-card.tsx"
      via: "import BlogCard"
      pattern: "import.*BlogCard.*from.*blog-card"
    - from: "apps/web/src/routes/blog/$slug.tsx"
      to: "apps/web/src/components/table-of-contents.tsx"
      via: "import TableOfContentsClient"
      pattern: "import.*TableOfContentsClient"
    - from: "apps/web/src/routes/blog/$slug.tsx"
      to: "apps/web/src/components/author-card.tsx"
      via: "import AuthorCard"
      pattern: "import.*AuthorCard"
---

<objective>
Restore visual parity for blog pages (list + article) between `apps/web` and `../website`.

Purpose: The blog list page has the biggest visual gap in the entire phase. `apps/web` uses a simple inline BlogCard while `../website` uses a sophisticated BlogCard with featured/compact variants, category headers, and proper hover effects. The article page is missing the grid layout with sidebar ToC, AuthorCard, and RelatedPosts.

Output: Blog list page with featured/compact BlogCard variants and category filter badges, blog article page with prose-neo typography, grid layout with sidebar ToC, AuthorCard, and RelatedPosts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-parity/09-CONTEXT.md
@.planning/phases/09-ui-parity/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlogCard component and update blog list page with category filters</name>
  <files>apps/web/src/components/blog-card.tsx, apps/web/src/routes/blog/index.tsx</files>
  <read_first>
    - ../website/components/blog/blog-card.tsx (reference BlogCard with featured/compact variants)
    - ../website/app/(main)/blog/page.tsx (reference blog list page structure)
    - ../website/components/blog/blog-list.tsx (reference BlogList with pinned/regular separation, category filter badges, search)
    - apps/web/src/routes/blog/index.tsx (current blog list page to modify)
    - apps/web/src/lib/notion.ts (BlogPost type definition, verify available fields including category)
  </read_first>
  <action>
1. Create `apps/web/src/components/blog-card.tsx` — a new component ported from `../website/components/blog/blog-card.tsx` with these adaptations:
   - Accept props `{ post: BlogPost; featured?: boolean }` where `BlogPost` is imported from `@/lib/notion`
   - **Featured variant** (when `featured={true}`):
     - Outer: `<article className="group col-span-full neo-border-thick neo-shadow bg-secondary hover:translate-x-0.5 hover:translate-y-0.5 hover:shadow-none transition-all">`
     - Inner: `<Link to="/blog/$slug" params={{ slug: post.slug }} className="flex flex-col md:flex-row">`
     - Left side: cover image in `<div className="relative aspect-video md:aspect-auto md:w-1/2 overflow-hidden border-b-2 md:border-b-0 md:border-r-2 border-foreground">` using `<img>` (NOT next/image `<Image>`), with fallback logo placeholder
     - Right side: `<div className="flex flex-1 flex-col p-6 md:p-8">` containing category label (`text-xs font-black uppercase tracking-wider text-primary`), title (`text-2xl md:text-3xl font-black line-clamp-2 group-hover:text-primary transition-colors`), description, author/date footer with ArrowRight CTA
     - Author avatar: `<img>` with `className="neo-border"` for avatar, or fallback div `h-9 w-9 items-center justify-center bg-primary text-primary-foreground text-sm font-black neo-border`
     - Date format: `toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric" })` (month: "short" not "long")
     - Footer right: `<span className="flex items-center gap-1 font-bold text-primary">Read <ArrowRight className="h-4 w-4" /></span>`
   - **Compact variant** (default, `featured` is false or undefined):
     - Outer: `<article className="group neo-border-thick neo-shadow bg-secondary hover:translate-x-0.5 hover:translate-y-0.5 hover:shadow-none transition-all">`
     - Inner: `<Link to="/blog/$slug" params={{ slug: post.slug }} className="flex flex-col h-full">`
     - Header bar: `<div className="flex items-center justify-between px-4 py-3 border-b-2 border-foreground bg-background">` with category name (text-xs font-black uppercase tracking-wider text-primary) and date (text-xs font-medium text-muted-foreground)
     - Content: `<div className="flex flex-col flex-1 p-4">` with title (`font-bold text-lg line-clamp-2 group-hover:text-primary transition-colors`), description (`mt-2 text-sm text-muted-foreground line-clamp-2 flex-1`)
     - Footer: `<div className="mt-4 pt-4 border-t border-border flex items-center justify-between">` with author avatar+name on left, `<ArrowRight className="h-4 w-4 text-primary group-hover:translate-x-1 transition-transform" />` on right
     - Author avatar compact: `h-6 w-6` with neo-border
   - Import `ArrowRight, Calendar` from `lucide-react`, `Link` from `@tanstack/react-router`
   - Replace all Next.js `<Image>` with `<img>`, replace `href` with `to` on Link
   - Replace all Next.js `<Link href=...>` with `<Link to="/blog/$slug" params={{ slug: post.slug }}>`
   - Use logo from `/logo.png` (public dir) for the featured fallback image

2. Update `apps/web/src/routes/blog/index.tsx`:
   - Remove the inline `BlogCard` function entirely
   - Import `{ BlogCard }` from `@/components/blog-card`
   - Separate posts into pinned and regular: `const pinnedPosts = posts.filter(p => p.isPinned)` and `const regularPosts = posts.filter(p => !p.isPinned)`
   - In the grid, render pinned posts first with `featured={index === 0 && pinnedPosts.length === 1}`, then regular posts with default (compact) variant
   - **Add category filter badges** (per RESEARCH.md line 172 — these ARE in scope):
     - Derive categories from the post list: `const categories = [...new Set(posts.map(p => p.category?.name).filter(Boolean))]`
     - Add a filter bar above the post grid with clickable category badges: each badge is a `<button>` or `<Badge>` with `onClick` toggling category filter state
     - Use `useState` to track selected category; filter `regularPosts` by selected category when one is active
     - Style badges to match reference: `<span className="px-3 py-1 text-xs font-black uppercase tracking-wider neo-border-thick cursor-pointer transition-all">` with active state adding `bg-primary text-primary-foreground` and inactive state `bg-background hover:bg-secondary`
     - Include an "All" badge that clears the filter
   - Keep the existing hero section, loader, and empty state unchanged — they already match the reference

Per D-04 (code diff approach), per D-06 (../website is source of truth), per D-08 (fix in apps/web).
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/blog-card.tsx` exists and exports `BlogCard`
    - `blog-card.tsx` contains `hover:translate-x-0.5 hover:translate-y-0.5 hover:shadow-none` (reference hover pattern)
    - `blog-card.tsx` contains `border-b-2 border-foreground bg-background` (compact header bar)
    - `blog-card.tsx` contains `ArrowRight` import from lucide-react
    - `blog-card.tsx` contains `col-span-full` (featured variant full-width)
    - `apps/web/src/routes/blog/index.tsx` imports from `@/components/blog-card`
    - `apps/web/src/routes/blog/index.tsx` does NOT contain an inline `function BlogCard` definition
    - `apps/web/src/routes/blog/index.tsx` contains `isPinned` (pinned/regular separation)
    - `apps/web/src/routes/blog/index.tsx` contains category filter state and badge rendering
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Blog list page renders BlogCard with featured (pinned) and compact (regular) variants matching ../website visual structure. Category filter badges derived from post list are displayed above the grid. Hover effects use shadow-none instead of neo-shadow-xl. Cards have category header bars and ArrowRight indicators.</done>
</task>

<task type="auto">
  <name>Task 2: Fix blog article page typography, category link, and add grid layout with sidebar ToC, AuthorCard, RelatedPosts</name>
  <files>apps/web/src/routes/blog/$slug.tsx, apps/web/src/components/table-of-contents.tsx, apps/web/src/components/author-card.tsx, apps/web/src/components/related-posts.tsx</files>
  <read_first>
    - ../website/app/(main)/blog/[slug]/page.tsx (reference article page — prose-neo wrapper, grid layout, category Link, AuthorCard, RelatedPosts, ToC sidebar)
    - ../website/components/blog/table-of-contents.tsx (reference ToC component)
    - ../website/components/blog/author-card.tsx (reference AuthorCard component)
    - ../website/components/blog/related-posts.tsx (reference RelatedPosts component)
    - apps/web/src/routes/blog/$slug.tsx (current article page)
    - apps/web/src/styles/globals.css (verify .prose-neo class exists)
    - apps/web/src/lib/notion.ts (check if getBlogPostWithContent returns headings; check if relatedPosts data is available)
  </read_first>
  <action>
1. **Port TableOfContentsClient** to `apps/web/src/components/table-of-contents.tsx`:
   - Port from `../website/components/blog/table-of-contents.tsx`
   - Replace Next.js patterns: `usePathname()` is not needed (TanStack Router provides route info differently — use `window.location.hash` or `useLocation` from `@tanstack/react-router`)
   - The component takes a `headings: { id: string; text: string; level: number }[]` prop
   - Renders a nested list of heading links with `<a href="#${heading.id}">` for in-page navigation
   - Active heading tracking via IntersectionObserver (port from reference)
   - If `apps/web`'s `getBlogPostWithContent` does NOT return headings, add headings extraction from the blocks array: scan blocks for `heading_1`, `heading_2`, `heading_3` types and extract `{ id, text, level }` from them in the route loader

2. **Port AuthorCard** to `apps/web/src/components/author-card.tsx`:
   - Port from `../website/components/blog/author-card.tsx`
   - Replace `<Image>` with `<img>`, `<Link href=...>` with `<Link to=...>` or `<a href=...>`
   - Component takes author info (name, avatar, bio) as props

3. **Port RelatedPosts** to `apps/web/src/components/related-posts.tsx`:
   - Port from `../website/components/blog/related-posts.tsx`
   - Replace Next.js patterns: `<Image>` with `<img>`, `<Link href=...>` with `<Link to="/blog/$slug" params={{ slug }}>`
   - If related posts data is not available from `apps/web`'s Notion client, derive related posts by filtering all blog posts by same category (excluding current post) and taking up to 3
   - If deriving related posts requires loading all posts in the article route, add `getBlogPosts()` call to the route loader alongside `getBlogPostWithContent()`

4. **Update article page layout** in `apps/web/src/routes/blog/$slug.tsx`:
   - **Wrap content in grid layout**: Change the article content area to use:
     ```tsx
     <div className="grid gap-8 sm:gap-12 lg:grid-cols-[1fr_250px] items-start">
       <div className="prose-neo max-w-none min-w-0">
         <BlockRenderer blocks={post.blocks} />
         <AuthorCard author={post.author} />
         <RelatedPosts posts={relatedPosts} currentSlug={post.slug} />
       </div>
       {headings.length > 0 && (
         <aside className="hidden lg:block sticky top-28">
           <TableOfContentsClient headings={headings} />
         </aside>
       )}
     </div>
     ```
   - **Add mobile ToC** above the grid (inside the article container but before the grid):
     ```tsx
     {headings.length > 0 && (
       <div className="lg:hidden mb-8">
         <TableOfContentsClient headings={headings} />
       </div>
     )}
     ```
   - **Make category badge a clickable link**: Change the category from a `<span>` to `<Link to="/blog" search={{ category: post.category.name }}>` with hover effect classes `hover:translate-x-0.5 hover:translate-y-0.5 hover:shadow-none transition-all`
   - **Apply prose-neo**: Change `<div className="max-w-none prose-lg">` to `<div className="prose-neo max-w-none min-w-0">`

Per D-04 (code diff), per D-06 (../website source of truth), per D-08 (fix in apps/web). Per RESEARCH.md Don't Hand-Roll section: port ToC, AuthorCard, RelatedPosts from reference, do not build from scratch.
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/app && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/table-of-contents.tsx` exists and exports `TableOfContentsClient`
    - `apps/web/src/components/author-card.tsx` exists and exports `AuthorCard`
    - `apps/web/src/components/related-posts.tsx` exists and exports `RelatedPosts`
    - `apps/web/src/routes/blog/$slug.tsx` contains `prose-neo max-w-none min-w-0`
    - `apps/web/src/routes/blog/$slug.tsx` contains `lg:grid-cols-[1fr_250px]` (grid layout)
    - `apps/web/src/routes/blog/$slug.tsx` contains `sticky top-28` (desktop sidebar ToC)
    - `apps/web/src/routes/blog/$slug.tsx` contains `lg:hidden` (mobile ToC)
    - `apps/web/src/routes/blog/$slug.tsx` contains `<Link` wrapping the category badge
    - `apps/web/src/routes/blog/$slug.tsx` imports `AuthorCard` and `RelatedPosts`
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Blog article page uses grid layout with desktop sidebar ToC (lg:grid-cols-[1fr_250px]), mobile collapsible ToC, prose-neo wrapper, clickable category badge, AuthorCard below content, and RelatedPosts below AuthorCard. All components ported from ../website reference.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes with no errors
- Blog list page structure: featured card has `col-span-full`, compact cards have category header bar
- Blog list page has category filter badges
- Blog article page uses grid layout with sidebar ToC on desktop
- Blog article page wraps BlockRenderer output in `prose-neo` class
- Blog article page shows AuthorCard and RelatedPosts below content
- No imports from `next/image` or `next/link` in modified files
- Per D-05: Run Playwright screenshot comparison of `/blog` and `/blog/{any-slug}` between `apps/web` (port 3000) and `../website` (port 3001) using `with_server.py`:
  ```bash
  python .agents/skills/webapp-testing/scripts/with_server.py \
    --server "cd ../website && pnpm dev --port 3001" --port 3001 \
    --server "cd apps/web && pnpm dev" --port 3000 \
    -- python scripts/compare_blog_screenshots.py
  ```
  The comparison script should capture `/blog` and `/blog/{slug}` from both apps and save screenshots to `/tmp/parity-09-01/` for visual diff review.
</verification>

<success_criteria>
Blog list and article pages in `apps/web` match `../website` in card layout (featured/compact variants), hover effects (shadow-none), category filter badges, typography (prose-neo), grid layout with sidebar ToC, AuthorCard, RelatedPosts, and category interaction (clickable badge). PARITY-01 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-parity/09-01-SUMMARY.md`
</output>
