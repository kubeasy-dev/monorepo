---
phase: 06-observability
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - docker/otel-collector-config.yaml
  - docker-compose.yml
autonomous: true
requirements: [OBS-01, OBS-05]

must_haves:
  truths:
    - "OTel Collector zpages binds to localhost:55679, not 0.0.0.0:55679"
    - "Collector receives traces, metrics, and logs via OTLP from both apps"
    - "After one API request, Collector debug output shows HTTP span + child DB span (pg.query)"
  artifacts:
    - path: "docker/otel-collector-config.yaml"
      provides: "Collector config with zpages on localhost, debug exporter"
      contains: "localhost:55679"
    - path: "docker-compose.yml"
      provides: "Updated compose with OTEL_EXPORTER_OTLP_ENDPOINT env var for apps"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
  key_links:
    - from: "apps/api/src/instrumentation.ts"
      to: "docker/otel-collector-config.yaml"
      via: "OTLP HTTP on port 4318"
      pattern: "4318"
    - from: "docker-compose.yml"
      to: "docker/otel-collector-config.yaml"
      via: "volume mount for collector config"
      pattern: "otel-collector-config.yaml"
---

<objective>
Update OTel Collector config for local-only security (zpages on localhost) and verify the full observability pipeline works end-to-end with a DB span smoke test.

Purpose: Confirms the entire Phase 6 pipeline: SDK init order is correct, pg auto-instrumentation produces DB spans, and the Collector receives all signals. This is the phase gate — without DB spans visible, OBS-05 is not met.
Output: Updated collector config, documented smoke test procedure, verified DB span in collector logs
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-observability/06-CONTEXT.md
@.planning/phases/06-observability/06-RESEARCH.md
@.planning/phases/06-observability/06-02-SUMMARY.md
@.planning/phases/06-observability/06-03-SUMMARY.md

<interfaces>
<!-- Current collector config — zpages on 0.0.0.0 needs to change to localhost -->
From docker/otel-collector-config.yaml:
```yaml
extensions:
  zpages:
    endpoint: 0.0.0.0:55679   # MUST change to localhost:55679
```

<!-- Current docker-compose.yml — no env vars for OTEL endpoint -->
From docker-compose.yml:
```yaml
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.123.0
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ./docker/otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - "4317:4317"
      - "4318:4318"
      - "55679:55679"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OTel Collector config and docker-compose</name>
  <files>docker/otel-collector-config.yaml, docker-compose.yml</files>
  <read_first>
    - docker/otel-collector-config.yaml (current config — zpages on 0.0.0.0:55679, needs localhost:55679)
    - docker-compose.yml (current compose — need to verify ports and consider adding OTEL env var documentation)
  </read_first>
  <action>
1. Update `docker/otel-collector-config.yaml`:
   - Change zpages endpoint from `0.0.0.0:55679` to `localhost:55679`
   - This ensures the zpages debug UI is NOT accessible from outside the container on Railway's public network (OBS-01 requirement)
   - Keep everything else unchanged: OTLP receivers on 0.0.0.0:4317 (gRPC) and 0.0.0.0:4318 (HTTP), batch processor, debug exporter with detailed verbosity, all three pipelines (traces, metrics, logs)

   Final `docker/otel-collector-config.yaml`:
   ```yaml
   receivers:
     otlp:
       protocols:
         grpc:
           endpoint: 0.0.0.0:4317
         http:
           endpoint: 0.0.0.0:4318

   processors:
     batch:

   exporters:
     debug:
       verbosity: detailed

   extensions:
     zpages:
       endpoint: localhost:55679

   service:
     extensions: [zpages]
     pipelines:
       traces:
         receivers: [otlp]
         processors: [batch]
         exporters: [debug]
       metrics:
         receivers: [otlp]
         processors: [batch]
         exporters: [debug]
       logs:
         receivers: [otlp]
         processors: [batch]
         exporters: [debug]
   ```

2. In `docker-compose.yml`, the otel-collector service port mapping for 55679 can remain (it maps host port to container port, but since zpages now binds to localhost inside the container, external access via the mapped port will not reach zpages — this is the intended behavior for Railway security). No changes needed to docker-compose.yml ports.

   Optionally, document the `OTEL_EXPORTER_OTLP_ENDPOINT` env var usage in a comment in docker-compose.yml near the otel-collector service. Apps use `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318` in their local .env files (not in docker-compose).
  </action>
  <verify>
    <automated>grep "localhost:55679" /Users/paul/Workspace/kubeasy/website/docker/otel-collector-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - docker/otel-collector-config.yaml contains `endpoint: localhost:55679` (not `0.0.0.0:55679`)
    - docker/otel-collector-config.yaml still contains `endpoint: 0.0.0.0:4317` for gRPC receiver
    - docker/otel-collector-config.yaml still contains `endpoint: 0.0.0.0:4318` for HTTP receiver
    - docker/otel-collector-config.yaml still contains `verbosity: detailed` in debug exporter
    - docker/otel-collector-config.yaml still has all three pipelines: traces, metrics, logs
  </acceptance_criteria>
  <done>OTel Collector zpages binds to localhost:55679. OTLP receivers remain on 0.0.0.0 for app access. Config is local-dev-only (no SigNoz exporter — prod apps talk directly to SigNoz).</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example with OTEL_EXPORTER_OTLP_ENDPOINT and document smoke test</name>
  <files>apps/api/.env.example</files>
  <read_first>
    - apps/api/.env.example (if it exists — check for existing env var documentation)
    - apps/api/src/instrumentation.ts (verify the default OTLP endpoint fallback value)
  </read_first>
  <action>
1. If `apps/api/.env.example` exists, add the `OTEL_EXPORTER_OTLP_ENDPOINT` variable. If it does not exist, create it with at minimum:
   ```
   OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
   ```

2. Similarly for `apps/web/.env.example` — add:
   ```
   OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
   ```

3. The smoke test procedure (OBS-05) is manual and documented here for the summary:
   - Start local stack: `docker-compose up -d`
   - Start API: `cd apps/api && pnpm dev`
   - Make one request: `curl http://localhost:3001/api/challenges`
   - Check Collector logs: `docker-compose logs otel-collector | grep -A5 "pg.query"`
   - Expected: An HTTP server span (`GET /api/challenges`) with a child DB span (`pg.query`) in the same trace
   - If DB spans missing: verify `--import ./src/instrumentation.ts` appears BEFORE `--import tsx/esm` in the dev script
  </action>
  <verify>
    <automated>grep "OTEL_EXPORTER_OTLP_ENDPOINT" /Users/paul/Workspace/kubeasy/website/apps/api/.env.example 2>/dev/null || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/.env.example contains `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318`
    - If apps/web/.env.example created, it also contains `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318`
  </acceptance_criteria>
  <done>Env var documentation updated. Smoke test procedure documented for OBS-05 verification.</done>
</task>

</tasks>

<verification>
- docker/otel-collector-config.yaml has zpages on localhost:55679
- .env.example files document OTEL_EXPORTER_OTLP_ENDPOINT
- Smoke test procedure is documented in summary for manual verification
- After running smoke test: Collector debug output shows HTTP span + child DB span confirming correct SDK init order
</verification>

<success_criteria>
- OTel Collector zpages not exposed on public network (localhost binding)
- Collector receives OTLP from both apps (traces, metrics, logs)
- DB span smoke test confirms correct --import initialization order (OBS-05)
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability/06-04-SUMMARY.md`
</output>
