---
phase: 05.1-repair-workers-features
plan: 05
subsystem: ui
tags: [react, tanstack-query, sse, eventsource, cache-invalidation]

# Dependency graph
requires:
  - phase: 05.1-repair-workers-features
    plan: 02
    provides: "SSE /api/sse/invalidate-cache endpoint with invalidate-cache events sending { queryKey }"
provides:
  - "Generic useInvalidateCacheSSE(enabled) hook in apps/web connecting to /api/sse/invalidate-cache"
  - "ChallengeMission updated to use generic SSE hook (no slug dependency)"
  - "Old useValidationSSE hook deleted — no validation-specific SSE remains"
affects: [05.1-repair-workers-features]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Generic SSE cache-invalidation hook — receives queryKey from server, calls invalidateQueries with it. No slug/resource specific binding."]

key-files:
  created:
    - apps/web/src/hooks/use-invalidate-cache-sse.ts
  modified:
    - apps/web/src/components/challenge-mission.tsx
    - apps/web/src/routes/blog/$slug.tsx
  deleted:
    - apps/web/src/hooks/use-validation-sse.ts

key-decisions:
  - "useInvalidateCacheSSE takes only enabled: boolean (no slug) — SSE channel is user-scoped, server decides which queryKey to invalidate"
  - "EventSource withCredentials: true required — auth cookie must be sent for protected /api/sse/invalidate-cache endpoint"

patterns-established:
  - "Generic SSE hook: receive queryKey from event data, call invalidateQueries — server drives what to invalidate, client is passive"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-03-19
---

# Phase 05.1 Plan 05: SSE Frontend Migration Summary

**Generic useInvalidateCacheSSE(enabled) hook replacing challenge-specific useValidationSSE — frontend SSE migration complete**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-19T22:00:00Z
- **Completed:** 2026-03-19T22:05:00Z
- **Tasks:** 1
- **Files modified:** 4 (1 created, 1 deleted, 2 modified)

## Accomplishments
- Created `apps/web/src/hooks/use-invalidate-cache-sse.ts` — generic hook subscribing to `/api/sse/invalidate-cache`, calls `invalidateQueries` with the `queryKey` received from each event
- Updated `ChallengeMission` to use `useInvalidateCacheSSE(status === "in_progress")` — slug parameter removed since channel is user-scoped
- Deleted `apps/web/src/hooks/use-validation-sse.ts` — no references to old hook remain anywhere in apps/web/src

## Task Commits

1. **Task 1: Create useInvalidateCacheSSE hook and replace useValidationSSE** - `424cce1ae` (feat)

**Plan metadata:** (pending)

## Files Created/Modified
- `apps/web/src/hooks/use-invalidate-cache-sse.ts` - New generic SSE hook, subscribes to /api/sse/invalidate-cache, calls invalidateQueries with received queryKey
- `apps/web/src/hooks/use-validation-sse.ts` - DELETED
- `apps/web/src/components/challenge-mission.tsx` - Updated SSE hook import/call; also fixed pre-existing DialogTrigger render prop bug
- `apps/web/src/routes/blog/$slug.tsx` - Fixed pre-existing trailing slash in Link to="/blog/"

## Decisions Made
- `useInvalidateCacheSSE` accepts only `enabled: boolean` — no slug needed since the API SSE channel is user-scoped (`invalidate-cache:{userId}`). The server publishes the specific `queryKey` to invalidate.
- `withCredentials: true` kept to send session cookie for authentication on the SSE endpoint.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed DialogTrigger render prop (invalid Radix UI API)**
- **Found during:** Task 1 (pre-commit typecheck)
- **Issue:** `DialogTrigger` in challenge-mission.tsx used a `render` prop (Base UI pattern), not supported by Radix UI's `DialogPrimitive.Trigger`. TypeScript error TS2322.
- **Fix:** Replaced `render={<Button .../>}` with `asChild` + `<Button>` as child containing the trigger content
- **Files modified:** `apps/web/src/components/challenge-mission.tsx`
- **Verification:** TypeScript typecheck passes with no errors
- **Committed in:** `424cce1ae` (Task 1 commit)

**2. [Rule 1 - Bug] Fixed /blog/ trailing slash in blog slug route Link**
- **Found during:** Task 1 (pre-commit typecheck)
- **Issue:** `to="/blog/"` in `apps/web/src/routes/blog/$slug.tsx` — trailing slash not a valid TanStack Router route. TypeScript error TS2820.
- **Fix:** Changed `to="/blog/"` to `to="/blog"`
- **Files modified:** `apps/web/src/routes/blog/$slug.tsx`
- **Verification:** TypeScript typecheck passes with no errors
- **Committed in:** `424cce1ae` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - pre-existing bugs caught by typecheck during commit)
**Impact on plan:** Both auto-fixes were pre-existing bugs discovered by the pre-commit typecheck hook. No scope creep.

## Issues Encountered
- Pre-commit Husky hook ran typecheck which caught two pre-existing TypeScript errors in files touched by this task. Both fixed inline as Rule 1 deviations.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- SSE frontend migration complete. The full SSE pipeline (API endpoint → publish on submission → browser cache invalidation) is now wired end-to-end.
- No blockers for next plan.

---
*Phase: 05.1-repair-workers-features*
*Completed: 2026-03-19*
