---
phase: 05.1-repair-workers-features
verified: 2026-03-19T22:05:28Z
status: passed
score: 18/18 must-haves verified
re_verification: false
---

# Phase 05.1: Repair Workers and Features — Verification Report

**Phase Goal:** Repair workers and features — refactor submit route to use BullMQ, rewrite all three workers with real business logic, create onboarding API routes, and update the web SSE hook to use generic cache-invalidation.
**Verified:** 2026-03-19T22:05:28Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | USER_SIGNIN queue name replaces USER_LIFECYCLE throughout packages and consumers | VERIFIED | `packages/jobs/src/queue-names.ts` exports `USER_SIGNIN`, no `USER_LIFECYCLE` present; `apps/api/src/lib/auth.ts` uses `userSigninQueue` + `QUEUE_NAMES.USER_SIGNIN` |
| 2 | ChallengeSubmissionPayload no longer contains xpAwarded | VERIFIED | `packages/jobs/src/payloads.ts` — payload has only `userId`, `challengeSlug`, `challengeId`, `difficulty`; no `xpAwarded` or `isFirstChallenge` |
| 3 | Query key factories are importable from @kubeasy/api-schemas/query-keys | VERIFIED | `packages/api-schemas/src/query-keys.ts` exists, exports `queryKeys`; `packages/api-schemas/package.json` exports map contains `"./query-keys": "./src/query-keys.ts"` |
| 4 | Onboarding schema has no webhook URL columns | VERIFIED | `apps/api/src/db/schema/onboarding.ts` contains only `id`, `userId`, `completedAt`, `skippedAt`, `cliAuthenticated`, `clusterInitialized`, `createdAt`, `updatedAt` |
| 5 | DB migration SQL is generated and committed | VERIFIED | `apps/api/drizzle/0000_glorious_tana_nile.sql` exists (11.3K), substantive SQL with CREATE TABLE statements |
| 6 | better-all is installed in apps/api | VERIFIED | `apps/api/package.json` contains `better-all` dependency |
| 7 | Submit route dispatches CHALLENGE_SUBMISSION job and returns { success, objectives } only | VERIFIED | `apps/api/src/routes/submit.ts` — no XP calculation code; dispatches `challengeSubmissionQueue.add("challenge-completed", ...)` at step 11; returns `{ success: true, objectives }` |
| 8 | SSE endpoint is generic cache-invalidation on /sse/invalidate-cache | VERIFIED | `apps/api/src/routes/sse.ts` — single route `GET /invalidate-cache`; subscribes to `invalidate-cache:${user.id}` Redis channel; emits event `invalidate-cache`; no old validation route |
| 9 | Submit route publishes to invalidate-cache:{userId} channel with queryKey payload | VERIFIED | `apps/api/src/routes/submit.ts` line 148-157 — publishes `{ queryKey: queryKeys.submissions.latest(challengeSlug) }` to `invalidate-cache:${userId}` |
| 10 | Auth databaseHooks dispatch USER_SIGNIN queue | VERIFIED | `apps/api/src/lib/auth.ts` — `userSigninQueue.add("user-signin", { userId, email, provider: "unknown" })` in `databaseHooks.user.create.after` |
| 11 | challenge-submission worker calculates isFirstChallenge, streak, XP, fires analytics, dispatches XP_AWARD jobs | VERIFIED | `apps/api/src/workers/challenge-submission.worker.ts` — counts `userXpTransaction` rows for `isFirstChallenge`, calls `calculateStreak`, `calculateXPGain`, `trackChallengeCompletedServer`, dispatches up to 3 `XP_AWARD` jobs |
| 12 | xp-award worker does DB upsert for userXp + inserts userXpTransaction + publishes SSE invalidation | VERIFIED | `apps/api/src/workers/xp-award.worker.ts` — atomic `onConflictDoUpdate` upsert on `userXp`, inserts `userXpTransaction`, publishes `{ queryKey: queryKeys.user.xp() }` to `invalidate-cache:${userId}` |
| 13 | user-signin worker runs identify/resend/trackSignup in parallel using better-all | VERIFIED | `apps/api/src/workers/user-lifecycle.worker.ts` — exports `createUserSigninWorker`, uses `all` from `better-all` for 3 parallel operations, stores `resendContactId` on success (revised approach — FlowProducer was superseded by better-all within Plan 03) |
| 14 | GET /api/onboarding returns currentStep (1-7), steps object, isComplete, isSkipped | VERIFIED | `apps/api/src/routes/onboarding.ts` — uses `better-all` for 4 parallel DB queries, computes `currentStep` 1-7, returns `{ steps, currentStep, isComplete, isSkipped }` |
| 15 | POST /api/onboarding/start/complete/skip all work | VERIFIED | All 3 POST routes exist in `onboarding.ts`: start uses `onConflictDoNothing`; complete sets `completedAt` + calls `trackOnboardingCompletedServer` + publishes SSE; skip sets `skippedAt` + calls `trackOnboardingSkippedServer` + publishes SSE |
| 16 | POST /api/cli/user and POST /api/cli/track/setup exist with analytics and SSE | VERIFIED | `apps/api/src/routes/cli/index.ts` — POST /user upserts `cliAuthenticated`, calls `trackCliLoginServer`, publishes SSE; POST /track/setup upserts `clusterInitialized`, calls `trackCliSetupServer`, publishes SSE |
| 17 | POST /progress/:slug/start fires trackChallengeStartedServer | VERIFIED | `apps/api/src/routes/progress.ts` line 228 — fire-and-forget call with `.catch()` |
| 18 | useInvalidateCacheSSE hook replaces useValidationSSE completely | VERIFIED | `apps/web/src/hooks/use-invalidate-cache-sse.ts` exists; `use-validation-sse.ts` deleted; `challenge-mission.tsx` imports and calls `useInvalidateCacheSSE`; no `useValidationSSE` references anywhere in `apps/web/src/` |

**Score:** 18/18 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/jobs/src/queue-names.ts` | USER_SIGNIN queue name constant | VERIFIED | Contains `USER_SIGNIN: "user-signin"`, no `USER_LIFECYCLE` |
| `packages/jobs/src/payloads.ts` | Updated payloads without xpAwarded, with UserSigninPayload | VERIFIED | `ChallengeSubmissionPayload` (4 fields, no xpAwarded), `XpAwardPayload` (with `description` + `challengeSlug`), `UserSigninPayload` (with `provider`) |
| `packages/api-schemas/src/query-keys.ts` | Shared query key factories | VERIFIED | Exports `queryKeys` with `submissions.latest`, `user.xp`, `onboarding` factories |
| `apps/api/src/db/schema/onboarding.ts` | Clean onboarding schema without webhook columns | VERIFIED | 8 columns only, no webhook URL columns |
| `apps/api/src/routes/submit.ts` | Refactored submit with BullMQ dispatch, no XP logic | VERIFIED | No XP imports or calculations; dispatches `CHALLENGE_SUBMISSION` job; returns `{ success, objectives }` |
| `apps/api/src/routes/sse.ts` | Generic cache-invalidation SSE endpoint | VERIFIED | Single route `/invalidate-cache`, dedicated ioredis subscriber, 30s heartbeat, `onAbort` cleanup |
| `apps/api/src/lib/auth.ts` | USER_SIGNIN queue dispatch | VERIFIED | `userSigninQueue` dispatches with `QUEUE_NAMES.USER_SIGNIN` and `provider` field |
| `apps/api/src/workers/challenge-submission.worker.ts` | XP orchestrator worker | VERIFIED | Full business logic: isFirstChallenge, streak, XP calculation, analytics, 3 XP_AWARD job types |
| `apps/api/src/workers/xp-award.worker.ts` | Atomic XP DB insert + SSE publish worker | VERIFIED | `onConflictDoUpdate` upsert + transaction insert + `redis.publish` with `queryKeys.user.xp()` |
| `apps/api/src/workers/user-lifecycle.worker.ts` | User signin flow worker | VERIFIED | `createUserSigninWorker` with `better-all` parallel operations (identify + resend + track), stores `resendContactId` |
| `apps/api/src/routes/onboarding.ts` | Onboarding API routes (GET, start, complete, skip) | VERIFIED | 4 routes, `better-all` parallel queries in GET, analytics + SSE publish in complete/skip |
| `apps/api/src/routes/cli/index.ts` | Updated CLI routes with POST /user and POST /track/setup | VERIFIED | Both routes use `zValidator(cliMetadataSchema)`, track analytics, publish SSE |
| `apps/api/src/routes/progress.ts` | Challenge start analytics | VERIFIED | Imports and calls `trackChallengeStartedServer` fire-and-forget |
| `apps/web/src/hooks/use-invalidate-cache-sse.ts` | Generic SSE hook that invalidates queries | VERIFIED | Connects to `/api/sse/invalidate-cache`, listens for `invalidate-cache` event, calls `invalidateQueries` with received `queryKey` |
| `apps/web/src/components/challenge-mission.tsx` | Updated component using new SSE hook | VERIFIED | Imports and calls `useInvalidateCacheSSE(status === "in_progress")` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `packages/jobs/src/payloads.ts` | `packages/jobs/src/queue-names.ts` | `QUEUE_NAMES.USER_SIGNIN` in JobPayload type | WIRED | `[K in typeof QUEUE_NAMES.USER_SIGNIN]: UserSigninPayload` present |
| `packages/api-schemas/src/query-keys.ts` | `packages/api-schemas/package.json` | exports map entry | WIRED | `"./query-keys": "./src/query-keys.ts"` in exports |
| `apps/api/src/routes/submit.ts` | `packages/jobs/src/queue-names.ts` | `QUEUE_NAMES.CHALLENGE_SUBMISSION` | WIRED | `createQueue(QUEUE_NAMES.CHALLENGE_SUBMISSION, ...)` at line 19 |
| `apps/api/src/routes/submit.ts` | `apps/api/src/routes/sse.ts` | Redis publish to `invalidate-cache` channel | WIRED | `redis.publish(sseChannel, ssePayload)` with `invalidate-cache:${userId}` |
| `apps/api/src/lib/auth.ts` | `packages/jobs/src/queue-names.ts` | `QUEUE_NAMES.USER_SIGNIN` | WIRED | `createQueue(QUEUE_NAMES.USER_SIGNIN, redis.options)` |
| `apps/api/src/workers/challenge-submission.worker.ts` | `apps/api/src/workers/xp-award.worker.ts` | Dispatches `XP_AWARD` jobs to queue | WIRED | `createQueue(QUEUE_NAMES.XP_AWARD, connection)` + `xpAwardQueue.add(...)` for base/streak/first-challenge |
| `apps/api/src/workers/xp-award.worker.ts` | `apps/api/src/routes/sse.ts` | Redis publish to `invalidate-cache` channel | WIRED | `redis.publish(channel, payload)` with `invalidate-cache:${userId}` and `queryKeys.user.xp()` |
| `apps/api/src/routes/onboarding.ts` | `apps/api/src/routes/index.ts` | Route mounting | WIRED | `routes.route("/onboarding", onboarding)` |
| `apps/api/src/routes/cli/index.ts` | `apps/api/src/lib/analytics-server.ts` | Analytics tracking calls | WIRED | `trackCliLoginServer` and `trackCliSetupServer` called at lines 79 and 119 |
| `apps/web/src/hooks/use-invalidate-cache-sse.ts` | `apps/api/src/routes/sse.ts` | EventSource to `/api/sse/invalidate-cache` | WIRED | `url = \`${apiBase}/api/sse/invalidate-cache\`` with `withCredentials: true` |
| `apps/web/src/components/challenge-mission.tsx` | `apps/web/src/hooks/use-invalidate-cache-sse.ts` | import `useInvalidateCacheSSE` | WIRED | Import at line 22, call at line 97 |

---

### Requirements Coverage

No requirement IDs were declared for this phase (all plans have `requirements: []`). Phase goal was verified through observable truths and artifact verification above.

---

### Anti-Patterns Found

No anti-patterns detected in any modified file. No TODOs, FIXMEs, placeholder returns, or stub handlers found.

**Note:** The Plan 03 artifact for `user-lifecycle.worker.ts` listed `contains: "FlowProducer"` in the PLAN frontmatter, but the plan body explicitly revised this to use `better-all` instead. The implementation correctly follows the revised approach (standard Worker + `better-all`). This is not a gap — it is the intended final state per the plan revision.

---

### Human Verification Required

The following behaviors cannot be verified programmatically:

#### 1. Redis publish/subscribe end-to-end

**Test:** Start a challenge, submit it, and verify the challenge-mission component updates without a page reload.
**Expected:** After submission, the submission status UI updates within ~1-2 seconds via SSE.
**Why human:** Requires running Redis, the API server, and the web app simultaneously.

#### 2. BullMQ worker processing

**Test:** Submit a challenge and verify XP is awarded to the user after the workers process the job.
**Expected:** User XP increases and a `userXpTransaction` record is created.
**Why human:** Requires a running Redis + BullMQ worker process.

#### 3. Onboarding step progression

**Test:** Create a new user, call POST /onboarding/start, create an API key, call POST /cli/user with metadata, call POST /cli/track/setup, start a challenge, complete a challenge — verify GET /onboarding returns `currentStep` 7 and `isComplete` false (or true after POST /onboarding/complete).
**Expected:** `currentStep` increments correctly through all 7 steps.
**Why human:** Requires live database and auth session.

---

## Summary

All 18 observable truths are verified. The phase goal is fully achieved:

- **BullMQ refactor:** Submit route dispatches `CHALLENGE_SUBMISSION` jobs and no longer contains any XP calculation logic.
- **Workers:** All three workers have real business logic — challenge-submission orchestrates XP calculation and dispatches up to 3 XP_AWARD jobs; xp-award performs atomic DB upserts and publishes SSE; user-signin runs identify/resend/track in parallel using `better-all`.
- **Onboarding API:** Four routes (GET, start, complete, skip) with full feature parity to the original Next.js tRPC implementation, including analytics tracking and SSE invalidation.
- **Generic SSE:** The validation-specific SSE hook and endpoint have been fully replaced with a generic `invalidate-cache` pattern throughout the stack (API route, web hook, component).
- **Foundation packages:** `@kubeasy/jobs` exports clean payload types and `USER_SIGNIN` queue name; `@kubeasy/api-schemas` exports shared query key factories; onboarding schema is clean.

---

_Verified: 2026-03-19T22:05:28Z_
_Verifier: Claude (gsd-verifier)_
