---
phase: 05.2-missing-dashboard-profile-features
plan: 02
subsystem: ui
tags: [react, tanstack-query, better-auth, api-tokens, profile, sonner]

# Dependency graph
requires:
  - phase: 05.1-repair-workers-features
    provides: "PATCH /user/name endpoint and DELETE /user/progress endpoint in Hono API"
  - phase: 03-authentication
    provides: "Better Auth client with apiKeyClient plugin"
provides:
  - "ProfileSettings component: edit first/last name via PATCH /user/name"
  - "ProfileApiTokens component: list/create/copy/delete API tokens via authClient.apiKey.*"
  - "ProfileDangerZone component: reset progress via DELETE /user/progress and delete account via authClient.deleteUser"
  - "api.user.resetProgress() method in api-client.ts"
  - "Profile route page wired with all 4 sections (3 + email preferences placeholder)"
affects: [05.2-03-email-preferences]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useSuspenseQuery for API token list with apiKey.list() result shape (data.apiKeys array)"
    - "useMutation with toast success/error for profile mutations"
    - "Newly created token one-time reveal flow with clipboard copy"
    - "Confirmation dialogs for destructive actions (Dialog + DialogClose)"

key-files:
  created:
    - apps/web/src/components/profile-settings.tsx
    - apps/web/src/components/profile-api-tokens.tsx
    - apps/web/src/components/profile-danger-zone.tsx
  modified:
    - apps/web/src/lib/api-client.ts
    - apps/web/src/routes/_protected/profile.tsx

key-decisions:
  - "authClient.apiKey.list() returns { apiKeys: [...], total, limit, offset } not an array — must extract .apiKeys"
  - "ProfileApiTokens uses useSuspenseQuery wrapped in Suspense in profile route"
  - "Delete account redirects via window.location.href = '/' after authClient.deleteUser()"

patterns-established:
  - "Profile sections: neo-brutalist cards with neo-border neo-shadow p-6"
  - "Danger zone: bg-red-50 neo-border border-red-600 neo-shadow shadow-red-600"
  - "Confirmation dialogs for all destructive actions"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-03-19
---

# Phase 05.2 Plan 02: Profile Page — Edit Name, API Tokens, Danger Zone Summary

**Profile page sections: edit name via PATCH /user/name, API token management via Better Auth apiKey client, and danger zone with reset progress + account deletion**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-19T22:44:30Z
- **Completed:** 2026-03-19T22:47:22Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Created three profile section components with correct API integrations and sonner toasts
- Wired all components into the profile route, replacing the static placeholder card
- Fixed authClient.apiKey.list() response shape issue (returns object not array) discovered during TypeScript check

## Task Commits

1. **Task 1: Create ProfileSettings, ProfileApiTokens, ProfileDangerZone + add resetProgress** - `b4016a7b2` (feat)
2. **Task 2: Wire profile components into profile route page** - `ba5ecce2b` (feat)

## Files Created/Modified
- `apps/web/src/components/profile-settings.tsx` - Edit first/last name form with useMutation + toast
- `apps/web/src/components/profile-api-tokens.tsx` - API token list/create/copy/delete with one-time reveal flow
- `apps/web/src/components/profile-danger-zone.tsx` - Reset progress + delete account with confirmation dialogs
- `apps/web/src/lib/api-client.ts` - Added resetProgress() calling DELETE /user/progress
- `apps/web/src/routes/_protected/profile.tsx` - Wired all 4 sections, removed old static card

## Decisions Made
- `authClient.apiKey.list()` returns `{ apiKeys: [...], total, limit, offset }` not an array — cast via `unknown` to extract `.apiKeys`
- `ProfileApiTokens` uses `useSuspenseQuery` and is wrapped in `<Suspense>` in the profile route for loading state
- Delete account redirects to "/" via `window.location.href` after `authClient.deleteUser()` succeeds

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed authClient.apiKey.list() return type mismatch**
- **Found during:** Task 1 (TypeScript pre-commit hook)
- **Issue:** `authClient.apiKey.list()` data is `{ apiKeys: ApiKey[], total, limit, offset }` not `ApiKey[]` — casting directly to `ApiKey[]` caused TS2352 error
- **Fix:** Cast via `unknown` intermediary then extract `.apiKeys` from the object
- **Files modified:** apps/web/src/components/profile-api-tokens.tsx
- **Verification:** `pnpm exec tsc --noEmit` passes with no errors
- **Committed in:** b4016a7b2 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 type bug)
**Impact on plan:** Necessary fix for type safety and correct runtime behavior. No scope creep.

## Issues Encountered
None beyond the apiKey.list() type fix above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Profile page fully functional with edit name, API tokens, and danger zone
- Plan 03 can add email preferences section using the placeholder already in place
- All TypeScript checks pass

---
*Phase: 05.2-missing-dashboard-profile-features*
*Completed: 2026-03-19*
