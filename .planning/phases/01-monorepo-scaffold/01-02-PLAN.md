---
phase: 01-monorepo-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/api-schemas/package.json
  - packages/api-schemas/tsconfig.json
  - packages/api-schemas/src/index.ts
  - packages/api-schemas/src/challenges.ts
  - packages/api-schemas/src/themes.ts
  - packages/api-schemas/src/progress.ts
  - packages/api-schemas/src/xp.ts
  - packages/api-schemas/src/submissions.ts
  - packages/api-schemas/src/auth.ts
  - packages/jobs/package.json
  - packages/jobs/tsconfig.json
  - packages/jobs/src/index.ts
  - packages/jobs/src/queue-names.ts
  - packages/jobs/src/payloads.ts
  - packages/jobs/src/factory.ts
  - package.json
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03, PKG-04]

must_haves:
  truths:
    - "api-schemas exports Zod schemas for all 6 domains (challenges, themes, progress, xp, submissions, auth)"
    - "api-schemas covers 100% of existing tRPC procedure input/output shapes"
    - "jobs exports queue names, JobPayload types, and createQueue factory"
    - "jobs has zero imports from any apps/ directory"
    - "Both packages typecheck independently"
  artifacts:
    - path: "packages/api-schemas/src/index.ts"
      provides: "Barrel export of all domain schemas"
      min_lines: 6
    - path: "packages/api-schemas/src/challenges.ts"
      provides: "Challenge domain Zod schemas"
      contains: "ChallengeListInputSchema"
    - path: "packages/api-schemas/src/submissions.ts"
      provides: "Submission domain Zod schemas"
      contains: "ObjectiveResultSchema"
    - path: "packages/jobs/src/queue-names.ts"
      provides: "BullMQ queue name constants"
      contains: "QUEUE_NAMES"
    - path: "packages/jobs/src/factory.ts"
      provides: "createQueue factory function"
      contains: "export function createQueue"
  key_links:
    - from: "packages/api-schemas/tsconfig.json"
      to: "packages/typescript-config/base.json"
      via: "extends"
      pattern: "@kubeasy/typescript-config/base.json"
    - from: "packages/jobs/tsconfig.json"
      to: "packages/typescript-config/base.json"
      via: "extends"
      pattern: "@kubeasy/typescript-config/base.json"
    - from: "packages/jobs/src/factory.ts"
      to: "packages/jobs/src/queue-names.ts"
      via: "import QueueName"
      pattern: "import.*QueueName.*queue-names"
---

<objective>
Create the two shared JIT packages: `@kubeasy/api-schemas` (Zod schemas covering all tRPC procedure shapes) and `@kubeasy/jobs` (BullMQ queue definitions and typed factory).

Purpose: Provides the type-safe contract layer that `apps/api` and `apps/web` will consume in later phases. The api-schemas package is the single source of truth for all API request/response shapes.
Output: `packages/api-schemas/` with 6 domain schema files and `packages/jobs/` with queue names, payloads, and factory.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-scaffold/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- Existing tRPC procedure shapes to port — these are the source of truth -->

From server/api/routers/challenge.ts:
- list: input = challengeFiltersSchema {difficulty?: "easy"|"medium"|"hard", type?: string, theme?: string, showCompleted?: boolean, search?: string}
- list: output = {challenges: ChallengeListItem[], count: number}
- getBySlug: input = {slug: string}, output = {challenge: ChallengeDetail | null}
- getObjectives: input = {slug: string}, output = {objectives: ObjectiveItem[]}
- create: input = {slug, title, description, theme, difficulty, estimatedTime, initialSituation, ofTheWeek}
- setAvailability: input = {slug: string, available: boolean}
- delete: input = {slug: string}
- adminList: output = {challenges: AdminChallengeItem[]}
- adminStats: output = {totalSubmissions, successfulSubmissions, successRate, totalStarts, totalCompletions, completionRate}

From server/api/routers/theme.ts:
- list: no input, output = Theme[]
- get: input = {slug: string}, output = Theme | undefined

From server/api/routers/userProgress.ts:
- getCompletionPercentage: input = {splitByTheme?: boolean, themeSlug?: string}
- getXpAndRank: no input, output = {xpEarned, rank, rankInfo}
- getStreak: no input, output = streak object
- completeChallenge: input = {challengeId: number}
- getStatus: input = {slug: string}, output = {status, startedAt?, completedAt?}
- startChallenge: input = {challengeSlug: string}
- submitChallenge: input = {challengeSlug: string, results: ObjectiveResult[]}
- resetChallenge: input = {challengeSlug: string}
- getSubmissions: input = {slug: string}
- getLatestValidationStatus: input = {slug: string}

From server/api/routers/xpTransaction.ts:
- getRecentGains: no input, output = XpTransaction[]

From schemas/cli-api.ts (reference shapes — copy patterns, not imports):
- objectiveResultSchema: {objectiveKey: string, passed: boolean, message?: string}
- objectiveSchema: {id: string, name: string, description?: string, passed: boolean, category: ObjectiveCategoryEnum, message?: string}
- objectiveCategorySchema: enum ["status", "log", "event", "metrics", "rbac", "connectivity"]

From server/db/schema/challenge.ts (enum values — inline in schemas, don't import):
- challengeDifficultyEnum: ["easy", "medium", "hard"]
- challengeStatusEnum: ["not_started", "in_progress", "completed"]
- xpActionEnum: ["challenge_completed", "daily_streak", "first_challenge", "milestone_reached", "bonus"]
- objectiveCategoryEnum: based on ObjectiveTypeSchema ["status", "condition", "log", "event", "connectivity"]
  NOTE: schemas/cli-api.ts uses ["status", "log", "event", "metrics", "rbac", "connectivity"] — use this version for api-schemas as it represents the API contract

From server/db/schema/auth.ts (user shape for auth schemas):
- user: {id: string, name: string, email: string, emailVerified: boolean, image?: string, role?: string, createdAt: Date, updatedAt: Date}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @kubeasy/api-schemas with all 6 domain schema files</name>
  <files>
    packages/api-schemas/package.json,
    packages/api-schemas/tsconfig.json,
    packages/api-schemas/src/index.ts,
    packages/api-schemas/src/challenges.ts,
    packages/api-schemas/src/themes.ts,
    packages/api-schemas/src/progress.ts,
    packages/api-schemas/src/xp.ts,
    packages/api-schemas/src/submissions.ts,
    packages/api-schemas/src/auth.ts
  </files>
  <read_first>
    - server/api/routers/challenge.ts (source of truth for challenge procedure shapes)
    - server/api/routers/theme.ts (source of truth for theme procedure shapes)
    - server/api/routers/userProgress.ts (source of truth for progress procedure shapes)
    - server/api/routers/xpTransaction.ts (source of truth for XP procedure shapes)
    - schemas/cli-api.ts (reference Zod shapes — copy patterns, remove .meta() and @/ imports)
    - schemas/challengeFilters.ts (challenge filter schema to port)
    - schemas/challengeObjectives.ts (ObjectiveTypeSchema enum values)
    - server/db/schema/challenge.ts (enum values to inline: difficulty, status, xpAction, objectiveCategory)
    - server/db/schema/auth.ts (user table shape for auth schemas)
    - packages/typescript-config/base.json (the tsconfig this package extends)
  </read_first>
  <action>
**1. Create `packages/api-schemas/package.json`:**
```json
{
  "name": "@kubeasy/api-schemas",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./challenges": "./src/challenges.ts",
    "./themes": "./src/themes.ts",
    "./progress": "./src/progress.ts",
    "./xp": "./src/xp.ts",
    "./submissions": "./src/submissions.ts",
    "./auth": "./src/auth.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@kubeasy/typescript-config": "workspace:*",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  }
}
```

**2. Create `packages/api-schemas/tsconfig.json`:**
```json
{
  "extends": "@kubeasy/typescript-config/base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*.ts"]
}
```

**3. Create `packages/api-schemas/src/challenges.ts`** covering all challenge router shapes:

Schemas to define (all field names match existing tRPC procedure shapes):

- `ChallengeDifficultySchema` = `z.enum(["easy", "medium", "hard"])`
- `ChallengeListInputSchema` = z.object with: difficulty (optional ChallengeDifficultySchema), type (optional string), theme (optional string), showCompleted (optional boolean default true), search (optional string)
- `ChallengeListItemSchema` = z.object with: id (number int), slug (string), title (string), description (string), theme (string), themeSlug (string), difficulty (ChallengeDifficultySchema), type (string), typeSlug (string), estimatedTime (number int), initialSituation (string), ofTheWeek (boolean), createdAt (z.coerce.date()), updatedAt (z.coerce.date()), completedCount (number int), userStatus (string nullable)
- `ChallengeListOutputSchema` = z.object with: challenges (array of ChallengeListItemSchema), count (number int)
- `ChallengeDetailSchema` = z.object with: id (number int), slug (string), title (string), description (string), theme (string), themeSlug (string), difficulty (ChallengeDifficultySchema), type (string), typeSlug (string), estimatedTime (number int), initialSituation (string), ofTheWeek (boolean), available (boolean), starterFriendly (boolean), createdAt (z.coerce.date()), updatedAt (z.coerce.date())
- `ChallengeGetBySlugOutputSchema` = z.object with: challenge (ChallengeDetailSchema nullable)
- `ChallengeObjectiveItemSchema` = z.object with: id (number int), objectiveKey (string), title (string), description (string), category (string), displayOrder (number int)
- `ChallengeGetObjectivesOutputSchema` = z.object with: objectives (array of ChallengeObjectiveItemSchema)
- `ChallengeCreateInputSchema` = z.object with: slug (string min 1), title (string min 1), description (string min 1), theme (string min 1), difficulty (ChallengeDifficultySchema), estimatedTime (number int positive), initialSituation (string min 1), ofTheWeek (boolean default false)
- `ChallengeCreateOutputSchema` = z.object with: success (boolean), challenge (z.any()), action (z.enum(["created", "updated"]))
- `ChallengeDeleteInputSchema` = z.object with: slug (string min 1)
- `ChallengeDeleteOutputSchema` = z.object with: success (boolean), message (string)
- `ChallengeSetAvailabilityInputSchema` = z.object with: slug (string), available (boolean)
- `SlugInputSchema` = z.object with: slug (string)

Export all schemas AND their inferred types (e.g., `export type ChallengeListInput = z.infer<typeof ChallengeListInputSchema>`)

CRITICAL: Do NOT import from `@/server/db/schema` or any `@/` path. Inline ALL enum values directly.

**4. Create `packages/api-schemas/src/themes.ts`:**
- `ThemeSchema` = z.object with: slug (string), name (string), description (string), logo (string nullable)
- `ThemeListOutputSchema` = z.array(ThemeSchema)
- `ThemeGetInputSchema` = z.object with: slug (string)

**5. Create `packages/api-schemas/src/progress.ts`:**
- `ChallengeStatusSchema` = z.enum(["not_started", "in_progress", "completed"])
- `CompletionPercentageInputSchema` = z.object with: splitByTheme (boolean default false optional), themeSlug (string optional) — use `.optional().default({splitByTheme: false})` on the outer object
- `ThemeCompletionSchema` = z.object with: themeSlug (string), completedCount (number), totalCount (number), percentageCompleted (number)
- `CompletionPercentageOutputSchema` = z.object with: completedCount (number), totalCount (number), percentageCompleted (number), byTheme (array of ThemeCompletionSchema optional)
- `XpAndRankOutputSchema` = z.object with: xpEarned (number), rank (string), rankInfo (z.object with name (string), progress (number), nextRankXp (number nullable))
- `StreakOutputSchema` = z.object with: currentStreak (number), lastActivityDate (string nullable)
- `CompleteChallengeInputSchema` = z.object with: challengeId (number)
- `CompleteChallengeOutputSchema` = z.object with: success (boolean), xpAwarded (number), baseXp (number), bonusXp (number), streakBonus (number), currentStreak (number), isFirstChallenge (boolean)
- `GetStatusInputSchema` = z.object with: slug (string)
- `GetStatusOutputSchema` = z.object with: status (ChallengeStatusSchema), startedAt (z.coerce.date() optional), completedAt (z.coerce.date() optional)
- `StartChallengeInputSchema` = z.object with: challengeSlug (string)
- `StartChallengeOutputSchema` = z.object with: status (z.enum(["in_progress", "completed"])), startedAt (z.coerce.date() optional), message (string optional)
- `ResetChallengeInputSchema` = z.object with: challengeSlug (string)
- `ResetChallengeOutputSchema` = z.object with: success (boolean), message (string)
- `GetSubmissionsInputSchema` = z.object with: slug (string)
- `LatestValidationStatusInputSchema` = z.object with: slug (string)
- `LatestValidationStatusOutputSchema` = z.object with: hasSubmission (boolean), validated (boolean), objectives (z.any() nullable), timestamp (z.coerce.date() nullable)

**6. Create `packages/api-schemas/src/xp.ts`:**
- `XpActionSchema` = z.enum(["challenge_completed", "daily_streak", "first_challenge", "milestone_reached", "bonus"])
- `XpTransactionSchema` = z.object with: id (number int), action (XpActionSchema), xpAmount (number int), description (string nullable), createdAt (z.coerce.date()), challengeId (number int nullable), challengeTitle (string nullable), challengeSlug (string nullable), challengeDifficulty (z.enum(["easy", "medium", "hard"]) nullable)

**7. Create `packages/api-schemas/src/submissions.ts`:**
- `ObjectiveCategorySchema` = z.enum(["status", "log", "event", "metrics", "rbac", "connectivity"])
- `ObjectiveResultSchema` = z.object with: objectiveKey (string), passed (boolean), message (string optional)
- `ObjectiveSchema` = z.object with: id (string), name (string), description (string optional), passed (boolean), category (ObjectiveCategorySchema), message (string optional)
- `ChallengeSubmitInputSchema` = z.object with: challengeSlug (string), results (array of ObjectiveResultSchema min 1)
- `ChallengeSubmitSuccessOutputSchema` = z.object with: success (z.literal(true)), xpAwarded (number), totalXp (number), rank (string nullable), rankUp (boolean), firstChallenge (boolean), streakBonus (number), currentStreak (number)
- `ChallengeSubmitFailureOutputSchema` = z.object with: success (z.literal(false)), message (string), failedObjectives (array of z.object with id (string), name (string), message (string))
- `ChallengeSubmitOutputSchema` = z.union of SuccessOutput and FailureOutput

**8. Create `packages/api-schemas/src/auth.ts`:**
- `UserSchema` = z.object with: id (string), name (string), email (string), emailVerified (boolean), image (string nullable), role (string nullable), createdAt (z.coerce.date()), updatedAt (z.coerce.date())
- `SessionSchema` = z.object with: id (string), userId (string), expiresAt (z.coerce.date()), token (string)
- `ErrorResponseSchema` = z.object with: error (string), details (string optional)

**9. Create `packages/api-schemas/src/index.ts`** — barrel re-export:
```typescript
export * from './challenges';
export * from './themes';
export * from './progress';
export * from './xp';
export * from './submissions';
export * from './auth';
```

**10. Run `pnpm install`** to resolve workspace dependencies, then run `pnpm --filter @kubeasy/api-schemas typecheck` to verify all schemas compile.
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/website && pnpm --filter @kubeasy/api-schemas typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/api-schemas/package.json `name` is `"@kubeasy/api-schemas"`
    - packages/api-schemas/package.json has `"exports"` with keys `.`, `./challenges`, `./themes`, `./progress`, `./xp`, `./submissions`, `./auth`
    - packages/api-schemas/package.json has `"peerDependencies": { "zod": "^4.0.0" }`
    - packages/api-schemas/tsconfig.json contains `"extends": "@kubeasy/typescript-config/base.json"`
    - packages/api-schemas/src/challenges.ts exports `ChallengeListInputSchema`, `ChallengeListItemSchema`, `ChallengeDetailSchema`, `ChallengeDifficultySchema`
    - packages/api-schemas/src/challenges.ts contains `z.enum(["easy", "medium", "hard"])` (inlined, not imported)
    - packages/api-schemas/src/themes.ts exports `ThemeSchema`
    - packages/api-schemas/src/progress.ts exports `CompletionPercentageInputSchema`, `ChallengeStatusSchema`, `StartChallengeInputSchema`, `GetStatusOutputSchema`, `LatestValidationStatusOutputSchema`
    - packages/api-schemas/src/xp.ts exports `XpTransactionSchema`, `XpActionSchema`
    - packages/api-schemas/src/submissions.ts exports `ObjectiveResultSchema`, `ObjectiveSchema`, `ObjectiveCategorySchema`, `ChallengeSubmitInputSchema`, `ChallengeSubmitOutputSchema`
    - packages/api-schemas/src/auth.ts exports `UserSchema`, `SessionSchema`, `ErrorResponseSchema`
    - packages/api-schemas/src/index.ts re-exports all 6 domain modules
    - No file in packages/api-schemas/src/ contains `from "@/` or `from "../../` (no Next.js path aliases)
    - `pnpm --filter @kubeasy/api-schemas typecheck` exits 0
  </acceptance_criteria>
  <done>@kubeasy/api-schemas provides Zod schemas covering 100% of existing tRPC procedure shapes across 6 domains, typechecks independently, and uses no @/ imports</done>
</task>

<task type="auto">
  <name>Task 2: Create @kubeasy/jobs with queue names, payloads, factory, and wire root deps</name>
  <files>
    packages/jobs/package.json,
    packages/jobs/tsconfig.json,
    packages/jobs/src/index.ts,
    packages/jobs/src/queue-names.ts,
    packages/jobs/src/payloads.ts,
    packages/jobs/src/factory.ts,
    package.json
  </files>
  <read_first>
    - server/db/schema/challenge.ts (xpActionEnum values for payload type alignment)
    - packages/typescript-config/base.json (tsconfig this package extends)
    - package.json (root package.json — will add workspace deps)
  </read_first>
  <action>
**1. Create `packages/jobs/package.json`:**
```json
{
  "name": "@kubeasy/jobs",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./queue-names": "./src/queue-names.ts",
    "./payloads": "./src/payloads.ts",
    "./factory": "./src/factory.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.71.0",
    "ioredis": "^5.10.0"
  },
  "devDependencies": {
    "@kubeasy/typescript-config": "workspace:*",
    "typescript": "catalog:"
  }
}
```

**2. Create `packages/jobs/tsconfig.json`:**
```json
{
  "extends": "@kubeasy/typescript-config/base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*.ts"]
}
```

**3. Create `packages/jobs/src/queue-names.ts`:**
```typescript
export const QUEUE_NAMES = {
  CHALLENGE_SUBMISSION: 'challenge-submission',
  XP_AWARD: 'xp-award',
} as const;

export type QueueName = (typeof QUEUE_NAMES)[keyof typeof QUEUE_NAMES];
```

**4. Create `packages/jobs/src/payloads.ts`:**
```typescript
import type { QUEUE_NAMES } from './queue-names';

export interface ChallengeSubmissionPayload {
  userId: string;
  challengeSlug: string;
  challengeId: number;
}

export interface XpAwardPayload {
  userId: string;
  challengeId: number;
  xpAmount: number;
  action: 'challenge_completed' | 'daily_streak' | 'first_challenge';
}

export type JobPayload = {
  [K in typeof QUEUE_NAMES.CHALLENGE_SUBMISSION]: ChallengeSubmissionPayload;
} & {
  [K in typeof QUEUE_NAMES.XP_AWARD]: XpAwardPayload;
};
```

Note: Use `typeof QUEUE_NAMES.CHALLENGE_SUBMISSION` (the literal string type) as the mapped key, not the QUEUE_NAMES import directly. Alternatively, define the map explicitly:
```typescript
import { QUEUE_NAMES } from './queue-names';

export type JobPayload = {
  [QUEUE_NAMES.CHALLENGE_SUBMISSION]: ChallengeSubmissionPayload;
  [QUEUE_NAMES.XP_AWARD]: XpAwardPayload;
};
```

**5. Create `packages/jobs/src/factory.ts`:**
```typescript
import { Queue } from 'bullmq';
import type IORedis from 'ioredis';
import type { QueueName } from './queue-names';
import type { JobPayload } from './payloads';

export function createQueue<N extends QueueName>(
  name: N,
  connection: IORedis,
): Queue<JobPayload[N]> {
  return new Queue(name, {
    connection,
    defaultJobOptions: {
      attempts: 3,
      backoff: { type: 'exponential', delay: 1000 },
      removeOnComplete: { count: 1000, age: 86400 },
      removeOnFail: { count: 5000, age: 604800 },
    },
  });
}
```

**6. Create `packages/jobs/src/index.ts`** — barrel re-export:
```typescript
export { QUEUE_NAMES } from './queue-names';
export type { QueueName } from './queue-names';
export type { ChallengeSubmissionPayload, XpAwardPayload, JobPayload } from './payloads';
export { createQueue } from './factory';
```

**7. Update root `package.json`** — add workspace dependencies:
- Add to `dependencies`: `"@kubeasy/api-schemas": "workspace:*"`
- Add to `dependencies`: `"@kubeasy/jobs": "workspace:*"`

These allow the root Next.js app to import from the packages.

**8. Run `pnpm install`** to resolve workspace deps, then `pnpm --filter @kubeasy/jobs typecheck`.

**9. Verify no apps/ imports:** Run `grep -r "apps/" packages/jobs/src/` — must return empty (PKG-04).

**10. Run `pnpm turbo:typecheck`** (or `turbo run typecheck`) from root to verify the entire workspace typechecks including package dependency resolution.
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/website && pnpm --filter @kubeasy/jobs typecheck && grep -r "apps/" packages/jobs/src/ | grep -v "node_modules" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/jobs/package.json `name` is `"@kubeasy/jobs"`
    - packages/jobs/package.json has `"dependencies"` with `"bullmq"` and `"ioredis"`
    - packages/jobs/package.json has NO dependency on any `@kubeasy/api-schemas` or apps/ package
    - packages/jobs/tsconfig.json contains `"extends": "@kubeasy/typescript-config/base.json"`
    - packages/jobs/src/queue-names.ts exports `QUEUE_NAMES` with keys `CHALLENGE_SUBMISSION` and `XP_AWARD`
    - packages/jobs/src/queue-names.ts exports `QueueName` type
    - packages/jobs/src/payloads.ts exports `ChallengeSubmissionPayload`, `XpAwardPayload`, `JobPayload`
    - packages/jobs/src/factory.ts exports `createQueue` function
    - packages/jobs/src/index.ts re-exports all public API
    - `grep -r "apps/" packages/jobs/src/` returns empty (no apps/ imports — PKG-04)
    - `pnpm --filter @kubeasy/jobs typecheck` exits 0
    - Root package.json contains `"@kubeasy/api-schemas": "workspace:*"` in dependencies
    - Root package.json contains `"@kubeasy/jobs": "workspace:*"` in dependencies
  </acceptance_criteria>
  <done>@kubeasy/jobs exports queue names, typed payloads, and createQueue factory with zero apps/ imports; root package.json has workspace:* deps for both packages</done>
</task>

</tasks>

<verification>
- `pnpm --filter @kubeasy/api-schemas typecheck` exits 0
- `pnpm --filter @kubeasy/jobs typecheck` exits 0
- `grep -r "from \"@/" packages/api-schemas/src/ packages/jobs/src/` returns empty (no Next.js path aliases)
- `grep -r "apps/" packages/jobs/src/` returns empty (PKG-04)
- `turbo run typecheck` passes for all workspace packages
</verification>

<success_criteria>
- @kubeasy/api-schemas covers all 6 domains with Zod schemas matching existing tRPC procedure shapes
- @kubeasy/jobs provides BullMQ queue names, typed payloads, and createQueue factory
- Both packages extend @kubeasy/typescript-config/base.json and typecheck independently
- No packages import from @/ paths or apps/ directories
- Root Next.js app can resolve both packages via workspace:* protocol
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffold/01-02-SUMMARY.md`
</output>
