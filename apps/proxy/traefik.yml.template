entryPoints:
  web:
    address: ":${PORT}"

# Enable ping internal service
ping:
  entryPoint: web

providers:
  file:
    filename: "${DYNAMIC_CONFIG_PATH}"
    watch: true

log:
  level: DEBUG

accessLog:
  format: json

tracing:
  otlp:
    grpc:
      endpoint: "${OTEL_COLLECTOR_GRPC_UPSTREAM}"
      insecure: true

# Disable dashboard and API to avoid port conflicts
api:
  dashboard: false
  insecure: false
